Lists files and directories in a tree structure.
Defaults to current directory if no path is specified.
Examples: ls (current dir), ls src, ls server/auth.py (specific file).
Use ignore parameter to exclude files (e.g., ["*.log", "node_modules"]).
For searching specific patterns, prefer Glob and Grep tools.