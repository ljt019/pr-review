ToolIndicator {
    height: auto;
    width: 100%;
    margin: 1 0;
    padding: 0 2;
    background: transparent;
    border: none;
    text-align: left;
    min-height: 1;
    max-height: 1;
    color: $text;
}